### Variables de entorno de ejemplo
# Copia este archivo a `.env` y reemplázalo con valores reales.

# Contraseña para acceder al panel administrador (cliente)
VITE_ADMIN_PASSWORD=Keisy5566@.
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
JWT_SECRET=your_jwt_secret
N8N_WEBHOOK_URL=https://n8n.srv993612.hstgr.cloud/webhook/credit-app-dxjcie
 
# Puerto del backend para desarrollo (si tu server usa `PORT` en .env, este valor puede coincidir)
# Vite usará `BACKEND_PORT` o `PORT` para configurar el proxy dev y reenviar `/api`.
BACKEND_PORT=5000
